days=0, times=0, temp=0......  
For days = 1 to 365
   For times = 1 to 10
       Input temp
       total1 = totl1 + temp
       If temp > highest then
          highest = temp
       Endif
       If temp < lowest then
          lowest = temp
       Endif
   Next times
   avg1 = total1 / 10
   output avg1

   total2 = total2 + total1
   total1 = 0
Next days
avg2 = total2 / (365*10)
output avg2, highest, lowest